-- This SQL script creates a database named hbtn_0c_0 in MySQL.
-- If the database already exists, it won't produce an error.

-- Create the database if it doesn't already exist.
CREATE DATABASE IF NOT EXISTS 'hbtn_0c_0';
